- [2026-04-25] [Seeder/Math]:
  - **Action**: Completed `generate_synthetic_month.py` and successfully generated 30 days of data (`synthetic_data.json`).
  - **Injected Heuristics**: 
    - Programmed a `-0.6` sentiment drop when `sleep_hours < 6` and injected the `"lack of sleep"` trigger. 
    - Programmed a `+0.4` boost when the user exercises, adding the `"gym"` tag and `"exercise"` trigger.
    - Added slight penalty (`-0.3` sentiment) for "academic pressure" randomly on weekdays.
  - **Math Engine**: The data is prepared with clear rankable signals to allow the planned Kendall's Tau algorithm to detect robust correlation scores for insight generation.
  - **Integration Demand**: Script outputs JSON matching the `JournalEntry` structure as defined in `docs/JSON_CONTRACT.md`. We expect the Infrastructure DB schema to support `jsonb` fields for `tags` and `triggers`.
