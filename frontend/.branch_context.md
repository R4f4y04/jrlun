- [2026-04-25] [Initialization & Mock Setup]:
  - **Action**: Initialized `AppTheme` with the Material 3 Deep Purple/Lavender palette. Created `JournalEntry` and `InsightModel` data classes. Built `MockJournalService` and `MockInsightService`. Scaffolded the `InsightCard` UI component using `AnimatedSwitcher` to display the AI insights.
  - **Local State**: Added `provider`, `dio`, and `syncfusion_flutter_charts` dependencies for future state management, networking, and visualization.
  - **Mocks Used**: `MockJournalService` returning dummy `List<JournalEntry>` and `MockInsightService` returning a dummy `InsightModel` exactly matching `docs/JSON_CONTRACT.md`.
  - **Integration Demand**: Waiting for backend to implement `POST /api/v1/entries`, `GET /api/v1/entries`, and `GET /api/v1/insights/current` returning exact structures defined in `JSON_CONTRACT.md`.

- [2026-04-25] [Phase 2 — Core Dashboard]:
  - **Action**: Built `JournalProvider` and `InsightProvider` (ChangeNotifier MVC). Created `SentimentChart` (Syncfusion SplineSeries, crosshair + tooltip enabled), `JournalInputField` (minimalist, multiline), `HistoricalEntriesFeed` (Cards + behavioral Chips). Assembled all components in `DashboardScreen`. Wired `main.dart` with `MultiProvider`, `AppTheme`, and `DashboardScreen`. All 4 `withOpacity` deprecation warnings resolved using `.withValues(alpha:)`.
  - **Local State**: `JournalProvider` owns `List<JournalEntry>` + loading state. `InsightProvider` owns `InsightModel?` + loading state. Each widget tree uses isolated `Consumer<T>` builders to prevent full-tree rebuilds.
  - **Mocks Used**: All providers consume `MockJournalService` and `MockInsightService`. No live API calls.
  - **Integration Demand**: Replace `MockJournalService`/`MockInsightService` with real `Dio`-backed HTTP services pointing at `POST /api/v1/entries`, `GET /api/v1/entries?limit=10&offset=0`, and `GET /api/v1/insights/current` when backend merges.
