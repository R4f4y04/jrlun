- [2026-04-26] [Database/Docker]:
  - **Action**: Generated migration `001_initial_schema.sql` and initialized `docker-compose.yml`.
  - **Schema Note**: Created `journal_entries` and `insights` tables. Enforced strict schema validation using `pg_jsonschema` for `tags` and `triggers` (JSONB arrays of strings) and `correlations` (JSONB array of objects).
  - **Indexing**: Composite B-tree indexes added for `user_id` + `created_at DESC` on `journal_entries` and `user_id` + `generated_at DESC` on `insights` to optimize rapid time-series visualization.
  - **Network Expose**: Supabase PostgreSQL exposed to internal Docker network `mindmirror-network` at `supabase-db:5432`. Local development exposed on `localhost:5432`.
